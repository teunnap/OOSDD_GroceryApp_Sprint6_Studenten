name: MAUI workflow

on:
  push:
    branches:
      - main
      - develop
      - release/**
      - feature/**
  pull_request:
    branches:
      - main
      - develop
      - release/**
      - feature/**

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Haal de repository op
        uses: actions/checkout@v4

      - name: Installeer .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Installeer MAUI workloads
        run: dotnet workload install maui-android

      - name: Herstel afhankelijkheden (alleen Android)
        run: dotnet restore --runtime android-arm64

      - name: Bouw de applicatie (alleen Android)
        run: dotnet build --configuration Release --framework net8.0-android --no-restore

      - name: Voer unit tests uit
        run: dotnet test --configuration Release --no-build --verbosity normal
